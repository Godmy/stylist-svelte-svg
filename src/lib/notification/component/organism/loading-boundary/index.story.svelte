<script lang="ts">
	import Story from '$stylist/playground/component/molecule/story/index.svelte';
	import type { InterfaceControllerSettings } from '$stylist/playground/type/struct/interface-controller-settings';
	import LoadingBoundary from './index.svelte';
	type Props = { loading: boolean; hasError: boolean };
	const controls: InterfaceControllerSettings[] = [
		{ name: 'loading', type: 'boolean', defaultValue: false },
		{ name: 'hasError', type: 'boolean', defaultValue: false }
	];
</script>

<Story
	id="organisms-loading-boundary"
	title="Organisms / Interaction / Feedback / Loading Boundary"
	component={LoadingBoundary}
	category="Organisms/Interaction/Feedback"
	{controls}
>
	{#snippet children(values: any)}{@const p = values as Props}
		<div class="sb-organisms-loading-boundary _c1">
			<LoadingBoundary loading={p.loading} error={p.hasError ? 'Request failed' : ''} />
		</div>{/snippet}
</Story>

<style>
	._c1 {
		padding: 1rem;
	}
</style>
