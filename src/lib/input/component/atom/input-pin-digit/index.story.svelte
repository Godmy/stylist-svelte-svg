<script lang="ts">
	import Story from '$stylist/playground/component/molecule/story/index.svelte';
	import type { InterfaceControllerSettings } from '$stylist/playground/type/struct/interface-controller-settings';
	import PinInputDigit from './index.svelte';

	const controls: InterfaceControllerSettings[] = [
		{ name: 'value', type: 'text', defaultValue: '4' },
		{ name: 'focused', type: 'boolean', defaultValue: false },
		{ name: 'invalid', type: 'boolean', defaultValue: false },
		{ name: 'size', type: 'select', defaultValue: 'md', options: ['sm', 'md', 'lg'] },
		{
			name: 'variant',
			type: 'select',
			defaultValue: 'default',
			options: ['default', 'outline', 'ghost']
		}
	];
</script>

<Story
	component={PinInputDigit}
	title="InputPinDigit"
	description="Single digit input used in OTP/PIN flows."
	{controls}
>
	{#snippet children(values: any)}
		<div class="_c1">
			<PinInputDigit
				id="pin-1"
				value={values.value}
				focused={values.focused}
				invalid={values.invalid}
				size={values.size}
				variant={values.variant}
			/>
			<PinInputDigit id="pin-2" value="" size={values.size} variant={values.variant} />
			<PinInputDigit id="pin-3" value="" size={values.size} variant={values.variant} />
			<PinInputDigit id="pin-4" value="" size={values.size} variant={values.variant} />
		</div>
	{/snippet}
</Story>

<style>
	._c1 {
		display: flex;
		gap: 0.5rem;
	}
</style>
