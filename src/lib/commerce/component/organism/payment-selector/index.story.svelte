<script lang="ts">
	import Story from '$stylist/playground/component/molecule/story/index.svelte';
	import type { InterfaceControllerSettings } from '$stylist/playground/type/struct/interface-controller-settings';
	import type { PaymentMethod } from '$stylist/commerce/type/struct/payment-method';
	import PaymentSelector from './index.svelte';

	const methods: PaymentMethod[] = [
		{ id: 'a', type: 'credit_card', name: 'Visa', details: '**** 1111', isDefault: true },
		{ id: 'b', type: 'credit_card', name: 'Mastercard', details: '**** 2222', isDefault: false }
	];

	type Props = { showAddButton: boolean; showSetDefault: boolean };
	const controls: InterfaceControllerSettings[] = [
		{ name: 'showAddButton', type: 'boolean', defaultValue: true },
		{ name: 'showSetDefault', type: 'boolean', defaultValue: true }
	];
</script>

<Story
	id="organisms-payment-selector"
	title="Organisms / Interaction / Controls / Payment / Payment Selector"
	component={PaymentSelector}
	category="Organisms/Interaction/Controls/Payment"
	{controls}
>
	{#snippet children(values: any)}
		{@const typedProps = values as Props}
		<div class="sb-organisms-payment-selector _c1">
			<PaymentSelector
				{methods}
				showAddButton={typedProps.showAddButton}
				showSetDefault={typedProps.showSetDefault}
			/>
		</div>
	{/snippet}
</Story>

<style>
	._c1 {
		padding: 1rem;
	}
</style>
