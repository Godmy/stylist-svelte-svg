<script lang="ts">
	import Story from '$stylist/playground/component/molecule/story/index.svelte';
	import type { InterfaceControllerSettings } from '$stylist/playground/type/struct/interface-controller-settings';
	import DataTableAdvanced from './index.svelte';

	const data = Array.from({ length: 23 }, (_, i) => ({
		id: i + 1,
		name: `User ${i + 1}`,
		team: i % 2 ? 'A' : 'B'
	}));
	const columns = [
		{ key: 'id', header: 'ID' },
		{ key: 'name', header: 'Name' },
		{ key: 'team', header: 'Team' }
	];

	type Props = { showSearch: boolean; showExport: boolean; loading: boolean };
	const controls: InterfaceControllerSettings[] = [
		{ name: 'showSearch', type: 'boolean', defaultValue: true },
		{ name: 'showExport', type: 'boolean', defaultValue: true },
		{ name: 'loading', type: 'boolean', defaultValue: false }
	];
</script>

<Story
	id="organisms-data-table-advanced"
	title="Organisms / Interaction / Controls / Tables / Data Table Advanced"
	component={DataTableAdvanced}
	category="Organisms/Interaction/Controls/Tables"
	{controls}
>
	{#snippet children(values: any)}
		{@const typedProps = values as Props}
		<div class="sb-organisms-data-table-advanced _c1">
			<DataTableAdvanced
				{data}
				{columns}
				showSearch={typedProps.showSearch}
				showExport={typedProps.showExport}
				loading={typedProps.loading}
			/>
		</div>
	{/snippet}
</Story>

<style>
	._c1 {
		padding: 1rem;
	}
</style>
