# Release Checklist `stylist-svelte` 1.0.1

Дата: 2026-05-11
Статус: **READY FOR RELEASE**

## 1. Подготовка кодовой базы
- [x] **Версия:** `1.0.1` зафиксирована в `package.json`.
- [x] **SvelteKit Dependency:** Полностью удалены импорты из `$app/environment`. Библиотека проверена на универсальность (успешная замена на `typeof window !== 'undefined'`).
- [x] **Индексация:** Запущена в режиме `package`. Публичный API (`src/lib/index.ts`) очищен от внутренних и экспериментальных доменов.
- [x] **Иконки:** Внутренний SVG-реестр стабилизирован, зависимость от `lucide-svelte` удалена.
- [x] **Форматирование:** Выполнен полный прогон `yarn format`.

## 2. Фильтрация и состав пакета
- [x] **Исключения доменов:** Режим `package` в `generation_domain_filter.py` настроен (исключены `commerce`, `marketing`, `science`, `chat` и др.).
- [x] **Режим Full:** Скорректирован, теперь исключает только `server`.
- [x] **Поле files в package.json:** Настроено для исключения тестов, сторибуков и приватных доменов из финального бандла.
- [x] **Dry-run проверка:** `npm pack --dry-run` показывает корректный состав файлов и оптимизированный размер (~7.3MB распакованный).

## 3. Качество и Валидация
- [x] **Build:** Проходит успешно (`yarn build`).
- [x] **Lint:** Ошибки отсутствуют.
- [x] **Типизация:** `svelte-check` и `tsc` проходят без критических замечаний для публичного API.

## 4. Документация
- [x] **CHANGELOG.md:** Описаны все ключевые изменения версии 1.0.1.
- [x] **README.md:** Обновлены инструкции по установке и описание стратегии иконок.

---

## 5. Финальные шаги (Действия)
- [ ] **Commit & Push:** Зафиксировать все изменения текущей сессии.
- [ ] **Git Tag:** Создать тег `v1.0.1`.
- [ ] **Publish:** Выполнить `npm publish` (после ручного подтверждения).
- [ ] **Smoke Test:** Установить опубликованную версию в пустой проект и проверить импорт базового компонента.

---
*Отчет сформирован Gemini CLI на основе анализа состояния репозитория.*
