# Release Checklist `stylist-svelte`

Дата: 2026-05-06

## 1. Версия и релизный scope

- [ ] Согласована целевая версия следующего релиза
- [ ] Подтверждено, что опубликованная на npm версия сейчас `1.0.0`
- [ ] Локальная версия в `package.json` соответствует целевому релизу
- [ ] Зафиксирован список изменений, входящих в релиз
- [ ] Подготовлен `CHANGELOG.md`

## 2. Иконки и удаление `lucide-svelte`

- [ ] Проверено, что `lucide-svelte` больше не используется в runtime-коде
- [ ] Проверено, что `lucide-svelte` не упоминается в build-скриптах и документации как актуальная зависимость
- [ ] Зафиксирован текущий источник иконок для библиотеки
- [ ] Описано, как иконки добавляются, обновляются и поддерживаются
- [ ] Проверено, что в пакет не попадает лишний объем неиспользуемых иконок

## 3. Состав npm-пакета

- [ ] Выполнен `npm pack --dry-run --json`
- [ ] Проверен текущий `unpackedSize`
- [ ] Проверено текущее количество файлов в tarball
- [ ] Из публикации исключены `index.story.svelte`, demo-файлы и development-only артефакты, если они не являются частью публичного контракта
- [ ] Проверено, что в пакет не попадают внутренние отчеты, служебные markdown-файлы и лишние generated artifacts
- [ ] Проверено, что `files` в `package.json` соответствует реальному publish intent

## 4. Сборка и обязательные проверки

- [ ] После изменений экспорта выполнен `python -u "d:\2026\code\template\stylist\indexation\cli.py"`
- [ ] Выполнен `python -u "d:\2026\code\template\stylist\errors\cli.py"`
- [ ] Проходит `yarn build`
- [ ] Проходит `yarn validate`
- [ ] Проходит `yarn test:unit`
- [ ] Проходит `yarn tsc`
- [ ] Проходит `yarn check`

## 5. Публичный API

- [ ] Проверен root export библиотеки
- [ ] Проверено, нет ли случайных экспортов внутренних сущностей
- [ ] Принято решение по поддержке subpath imports
- [ ] Проверено, что generated barrels не нарушают ожидаемую экспортную поверхность
- [ ] Подтверждено, что API соответствует ожиданиям для выбранного semver-шага

## 6. Документация

- [ ] README описывает установку
- [ ] README описывает peer dependencies
- [ ] README содержит короткий рабочий пример использования
- [ ] README описывает требования к theming/runtime окружению
- [ ] README отражает текущую стратегию иконок
- [ ] Нет битых ссылок в документации
- [ ] `CONTRIBUTING.md` согласован с фактическим набором файлов
- [ ] Если есть ссылка на `CODE_OF_CONDUCT.md`, файл существует или ссылка удалена

## 7. CI и prepublish

- [ ] Проверено, что `prepublishOnly` запускает корректную последовательность шагов
- [ ] Проверено, что CI зеленый на релизном коммите
- [ ] Проверено, что generated files committed и не расходятся с рабочим деревом
- [ ] Проверено, что релиз не зависит от случайных локальных артефактов

## 8. Финальная проверка перед публикацией

- [ ] Версия в `package.json` окончательно подтверждена
- [ ] Финальный `npm pack --dry-run --json` проверен повторно
- [ ] Проверен итоговый размер пакета по сравнению с предыдущим релизом
- [ ] Проверено, что tarball содержит только ожидаемые файлы
- [ ] Подготовлен текст release notes

## 9. Публикация и post-publish

- [ ] Выполнен `npm publish`
- [ ] На npm отображается ожидаемая новая версия
- [ ] На npm отображается ожидаемый `unpackedSize`
- [ ] На npm отображается ожидаемое количество файлов
- [ ] Проверен install smoke test для опубликованного пакета
- [ ] Проверен минимальный импорт и базовый runtime сценарий
- [ ] Зафиксированы итоги релиза и все оставшиеся follow-up задачи
